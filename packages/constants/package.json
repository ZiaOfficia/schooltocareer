{
  "name": "@stc/constants",
  "version": "0.1.0",
  "private": true,
  "description": "Single source of truth: routes, permissions, roles, cache tags, SEO defaults and reference data. Zero runtime dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "lint": "eslint src", "typecheck": "tsc --noEmit" },
  "dependencies": { "@stc/types": "workspace:*" },
  "devDependencies": { "typescript": "^5.7.3" }
}
